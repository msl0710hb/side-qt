### 项目
- 利用 lambda 表达式实现自定义 sort

### 命令行
- clang++ -std=c++17 -Wall -Wextra -pedantic test.cpp -o test
- ./test

### 文件添加 Git 仓库
1. 把 day2 目录添加进 Git 仓库（已存在可跳过）
```cpp
cd /Users/klein_lin/CODE/side-qt
git add day2
```
2. 写一条新提交
```cpp
git commit -m "文件描述"
```
3. 推到 GitHub
```cpp
git push origin main
```

- 